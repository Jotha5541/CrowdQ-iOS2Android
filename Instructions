1) Deny any byte array in Java
2) Avoid substring churn
    - Will run on every scan callback
    - Implement CrowdQExchangeTag as Java enum and pojo
        = Implementation acts as main optimization
3) Getters and Setters to.string equal (no inheritance)
4) CBPeripheralManager.startAdvertising(adData)
5) Packet in service data, not in device manager
6) Recommended mapping:
    iOS broadcastUUID → Android AdvertiseData.addServiceUuid(ParcelUuid)
    iOS “local name = packed string” → Android:
    AdvertiseData.addServiceData(ParcelUuid(serviceUuid), packetBytes)
    where packetBytes is pack() encoded as ASCII/UTF‑8 bytes (or a tighter binary format if you decide to optimize later)
7) Need fragmentation for larger data payloads (switch to GATT?)

8) IN BRIDGE OBSERVER:
    Extract packet from service data
    Parse data to CrowdQ Exchange
    Enforce ordering using last-sequence logic
    Dispatch handling onto single-thread executor (to preserve ordering)

9) In Android 12+, there is requirement to handle runtime permissions depending on SDK device

10) Media Effects Pipeline - convert iOS to Android equivalent
    Queue effect -> execute -> sleep duration parameter -> cleanup (end queue)
    Switch to Android: without blocking where possible in pipeline
    Optimization:
        removing sleep method
        schedule callbacks to keep threads free (responsiveness underload improves when receiving BLE burst packets)
